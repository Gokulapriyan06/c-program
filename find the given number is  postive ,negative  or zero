#include <stdio.h>

int main() {

    int number;
    printf("Enter an integer: ");
    scanf("%d", &number);
    if (number > 0) {
        printf("%d is a positive integer.\n", number);
    }
    else if (number < 0) {
        printf("%d is a negative integer.\n", number);
    }
    else {
        printf("%d is zero.\n", number);
    }

    return 0;
}

output

Enter an integer: -1
-1 is a negative integer.

Enter an integer: 1
-1 is a positivetive integer.
