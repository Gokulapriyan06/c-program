// write a c program to copy from one array to another array 

#include <stdio.h>

int main() {
    
    int n;

    printf("Enter the size of an array: ");
    scanf("%d", &n);

    int arr1[n], arr2[n];

    printf("Enter the elements:\n");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr1[i]);
    }

    // Copy elements from arr1 to arr2
    for (int i = 0; i < n; i++) {
        arr2[i] = arr1[i];
    }

    // Print copied elements
    printf("Copied elements in array 2:\n");
    for (int i = 0; i < n; i++) {
        printf("%d\n", arr2[i]);
    }

    return 0;
}

output

Enter the size of an array: 5
Enter the elements:
10 20 30 40 50
Copied elements in array 2:
10
20
30
40
50
