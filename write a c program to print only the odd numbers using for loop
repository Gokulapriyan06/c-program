#include<stdio.h>

int main(){
    int i;
    printf("Enter the numbers from 1 to 10 :\n");
    for(int i=1; i<=10; i++){
        if(i%2==1){ // if(i%2!=0) - you can use this also
            printf("%d ", i);
        }
    }
    return 0;
