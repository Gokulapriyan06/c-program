 find the target element using binary search


#include<stdio.h>
#include<stdbool.h>
int main(){
    int n, target;
    
    int arr[n];  // creating size of an array 
    
    printf("Enter the size of an array :\n");
    scanf("%d", &n);
    
    printf("Enter the array elements: \n");
    for(int i=0; i<n; i++){
        scanf("%d", &arr[i]);
    }
    
    printf("Enter your target element:\n");
    scanf("%d", &target);
    
    
    int lb = 0;
    int ub = n-1;

    bool found = false;

    while(lb<=ub){
        int mid = lb+(ub-lb)/2;
        
        if(arr[mid]==target){
            printf("Your target element found : %d \n", mid);
            found = true;
            break;
        }
        else if(arr[mid]<target){
            lb = mid+1;
        }
        else{
            ub = mid-1;
        }
    }
    
    if(!found){
    printf("Element not found \n");
    }
    return 0;


Output

Enter the size of an array :
5
Enter the array elements: 
10 20 30 40 50
Enter your target element:
30
Your target element found : 
